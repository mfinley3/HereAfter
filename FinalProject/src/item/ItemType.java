package item;

/**
 * The Enum for ItemType. Used when creating and identifying what the item is
 * upon creation, picking up and use.
 */
public enum ItemType {
	ATK, DEF, HP, NONE, MEDKIT, GRENADE, MINE;
}
